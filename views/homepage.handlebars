<main>
    <div class="p-3">
        <h2 class="text-center text-light">Featured Games</h2>
        <ol class="container ">
        {{#each games as |game|}}
        {{/each}}
        </ol>    
    </div>
</main>